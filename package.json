{
  "name": "root",
  "private": true,
  "workspaces": [
    "example",
    "r3f-vfx"
  ],
  "scripts": {
    "dev": "run-p dev:*",
    "dev:library": "bun run -F r3f-vfx dev",
    "dev:example": "bun run -F example dev",
    "start": "bun run -F example start",
    "build": "bun run -F '*' build",
    "format": "prettier . --write"
  },
  "devDependencies": {
    "npm-run-all": "4.1.5",
    "prettier": "3.8.1"
  }
}
